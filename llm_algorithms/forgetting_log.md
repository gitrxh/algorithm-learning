# LLM 算法 · 遗忘记录表

[返回本部分](README.md) · [复习规则](../notes/review_guide.md) · [空白模板](../templates/forgetting_log_template.md)

记录范围：公式、张量维度、mask、头共享关系或 PyTorch 操作。对象填写模块 / 算法与笔记，每行只记录一个能够自测的遗忘点。复习历史只在实际闭卷复习后填写。

## 遗忘与复习记录

| 编号 | 对象 / 链接 | 发现日期 | 具体遗忘点 | 纠正要点 | 复习历史（日期：评分） | 下次复习 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| LLM-001 | [ReLU](pytorch_basics/relu.md) | 2026-09-24 | Python `max(0, x)` 与张量逐元素 ReLU 有什么区别？ | 用 `x * (x > 0)`、`torch.clamp(x, min=0)` 或 `F.relu(x)` 进行逐元素计算 | — | 2026-09-25 | 待复习 |
| LLM-002 | [ReLU](pytorch_basics/relu.md) | 2026-09-24 | 两种手写表达在 `x=0` 的梯度一样吗？ | 本地 PyTorch 2.4.0：掩码写法为 0，`clamp(min=0)` 为 1 | — | 2026-09-25 | 待复习 |

## 更新约定

- 下一个编号从 `LLM-003` 开始递增。
- 先合上答案尝试，再评分：`0 = 想不起来 / 做错`，`1 = 需要提示`，`2 = 独立完成并解释`。
- 同一遗忘点再次出现，在原行追加复习历史并更新纠正要点、下次日期与状态；不同卡点另开一行。
- 日期用 `YYYY-MM-DD`，多次复习用 `<br>` 分隔；“下次复习”由自己按[复习规则](../notes/review_guide.md)填写。
- 状态使用 `待复习 / 巩固中 / 已掌握`；已掌握的条目保留，方便以后回看。
